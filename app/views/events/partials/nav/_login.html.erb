<div class="login">
  <% if user_signed_in? %>
    <%= link_to('Logout', destroy_user_session_path, {class: "button", method: :delete}) %>
  <% else %>
    <%= link_to('Staff', new_user_session_path, {class: "button"})  %>
  <% end %>
</div>
